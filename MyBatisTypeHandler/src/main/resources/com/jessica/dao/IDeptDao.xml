<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jessica.dao.IDeptDao">
  <resultMap id="DeptResultMap" type="Dept">
    <result column="flag" property="flag" typeHandler="com.jessica.handler.BooleanTypeHandler"/>
  </resultMap>
  <select id="findAll" resultMap="DeptResultMap">
    select * from dept2
  </select>
  <select id="findByDeptNo" resultType="Dept">
    select * from dept2 where depNo = #{depNo}
  </select>
  <insert id="insertDept" parameterType="Dept" keyProperty="depNo">
    <selectKey resultType="Integer" keyProperty="depNo" order="BEFORE">
      select max(depNo)+1 from dept2
  </selectKey>
    insert into dept2 (depNo, depName, loc, flag) values(#{depNo}, #{depName}, #{loc}, #{flag})
  </insert>

</mapper>